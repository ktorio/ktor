description = ''
kotlin {
    sourceSets {
        commonMain {
            dependencies {
                api project(':ktor-utils')
            }

        }
        jvmMain {
            dependencies {
                api group: 'com.google.code.gson', name: 'gson', version: gson_version
            }
        }
    }
}
