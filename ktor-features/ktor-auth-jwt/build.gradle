description = ''

kotlin {
    sourceSets {
        commonMain {
            dependencies {
                api project(':ktor-features:ktor-auth')
                api group: 'com.googlecode.json-simple', name: 'json-simple', version: '1.1.1'
                api group: 'com.auth0', name: 'java-jwt', version: '3.4.0'
                api group: 'com.auth0', name: 'jwks-rsa', version: '0.5.0'
            }
        }
        commonTest {
            dependencies {
                api 'com.nhaarman:mockito-kotlin:1.6.0'
            }
        }
    }
}