kotlin {
    sourceSets {
        darwinMain { dependsOn posixMain }
        darwinTest { dependsOn darwinMain }

        configure([iosArm32Main, iosArm64Main, iosX64Main, macosX64Main]) {
            dependsOn darwinMain
        }
    }
}
