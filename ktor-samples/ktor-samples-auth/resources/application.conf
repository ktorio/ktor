ktor {
    deployment {
        port = 8080
    }

    application {
        modules = [ org.jetbrains.ktor.samples.auth.JwtAuthApplicationKt.JWTAuthApplication ]
    }
}

jwt {
    domain = "https://jwt-provider-domain/"
    audience = "jwt-audience"
}