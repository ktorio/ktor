description = "Ktor client GSON serializer for JSON support"

dependencies {
    compile project(":ktor-client:ktor-client-features:ktor-client-json")

    compile group: 'com.google.code.gson', name: 'gson', version: '2.8.2'
}
